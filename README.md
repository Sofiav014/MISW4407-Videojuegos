# Practica Taller Semana 4

## Generar Ejecutable

En la raiz del proyecto, corre el siguiente comando:

```shell
pyinstaller --noconsole --onefile main.py
```

Recuerda poner la carpeta `assets` en la carpeta `dist`